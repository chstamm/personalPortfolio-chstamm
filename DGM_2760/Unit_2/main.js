document.querySelector('#name').innerText = "Inspector Panda"
document.querySelector('#slogan').innerText = "Keeping Pandamonium to a Minimum"

let windowSizes = `My window size is currently ${window.innerWidth} by ${window.innerHeight}.`

let windowInfo = document.querySelector('#windowInfo').innerText = windowSizes

let windowOffset = `My window is offset by `

let offsetInfo = document.querySelector('#offsetInfo').innerText = windowOffset